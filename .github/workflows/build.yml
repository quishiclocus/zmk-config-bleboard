name: Build Firmware

on:
  push:
    branches:
      - 'main'
      - '!release'
  pull-request:
    branches:
      - '*'
      - '!release*'
  workflow_dispatch:
    branches:
      - '*'
      - '!release*'

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
